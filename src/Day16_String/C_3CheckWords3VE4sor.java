package Day16_String;

import jdk.swing.interop.SwingInterOpUtils;

import java.util.Scanner;

public class C_3CheckWords3VE4sor {
    public static void main(String[] args) {
       /* 3. write a method that asks user to enter a string.
        if the string is empty, print: string is empty
        if the string has more than 3 characters, print the last three characters
        if the string has less than 3 characters, print the string itself*/

      /* String str= " I like Java";

       if(str.length()==0){

           System.out.println("empty string");
       }  else if (str.length()>3 ){
           System.out.println(str.substring(str.length()-3));
       }else{
           System.out.println(str);
       }*/

    /*  String Str = "I like Java";
              if(Str.length() == 0 ) {
                  System.out.println("empty");
              }else if (Str.length() >3 ){
                  System.out.println();
              }

        // String result = (str.length()==0 )?"empty string" : ? (str.substring(str.length()-3)) : str;


      /*  4. Write a program  for CheckWords:
        Program accepts 3 words and :
        - if they are same length:      print "All words are same length"
                - if some same length and others not:    print "Not Same nor Different lengths"
                - if all different length :  print "All different length"*/

        // soru 4. soru
        /*String str1= "abcd";
       String str2 = "ab";
       String str3 = "abc";
       str1 = str1.concat(str2+str3);
       System.out.println(str1);// ucunu concat yapar.*/

       String adim= "fatih";
       adim= adim.toUpperCase();
        System.out.println(adim);




         }

}
